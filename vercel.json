{
  "rewrites": [
    {
      "comment": "Everything under /api that is not its own function file falls through to the Express app in api/index.js. The negative lookahead keeps this rule from swallowing the real functions -- notably the dynamic route api/generations/[id].js, which was being answered by Express's 404 instead.",
      "source": "/api/(?!index$|generate$|keys/add$|webhooks/razorpay$|generations/).*",
      "destination": "/api/index"
    }
  ]
}
